<%#
  TODO: everything here (probably) should move to the ContentSerializer so we don't have this
        heavy logic in a random partial
%>
<div id="in_this_universe_panel" class="row panel">
  <% @sidelinks_in_this_universe_count = 0 %>
  <%= render partial: 'content/contexts/universe', locals: { content: content } %>
</div>
