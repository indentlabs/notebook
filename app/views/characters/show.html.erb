<% content_for :content_show_footer do %>
  <%= link_to 'Edit this character', edit_character_path(@content), class: 'btn' %>
<% end %>
<%= render partial: 'content/show', locals: { content: @content } %>
