<% @feed.each do |share| %>
  <%= render partial: 'stream/share', locals: { share: share } %>
  <blockquote>
    ID: <%= share.id %>
  </blockquote>
<% end %>

<% if @feed.empty? %>
  We're all caught up!
<% end %>