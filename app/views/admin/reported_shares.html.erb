<% @feed.each do |share| %>
  <%= render partial: 'content_page_shares/stream_page_share', locals: { share: share, content: share.content_page } %>
  <blockquote>
    ID: <%= share.id %>
  </blockquote>
<% end %>

<% if @feed.empty? %>
  We're all caught up!
<% end %>