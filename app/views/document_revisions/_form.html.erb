<%= form_with(model: document_revision, local: true) do |form| %>
  <% if document_revision.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(document_revision.errors.count, "error") %> prohibited this document_revision from being saved:</h2>

      <ul>
        <% document_revision.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
