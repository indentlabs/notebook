<h4>Create a new location</h4>
<%= form_for @content, :url => location_create_process_path do |form| %>
	<%= render partial: 'content/form', locals: { f: form, content: @content } %>
	<%= form.submit nil, :class => 'btn btn-primary' %>
<% end %>